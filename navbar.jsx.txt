export default function Navbar({ activeTab, setActiveTab }) { return ( <nav className="flex justify-around border-b border-gray-200 sticky top-14 z-10 bg-white"> {['Tasks', 'Marketplace', 'Learning'].map((tab) => ( <button key={tab} onClick={() => setActiveTab(tab)} className={`py-3 px-2 text-center flex-1 font-medium ${ activeTab === tab ? 'border-b-2 border-[#007A5A] text-[#007A5A]' : 'text-gray-500' }`} > {tab} </button> ))} </nav> ); }